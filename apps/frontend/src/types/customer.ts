export type Customer = { id: number; name: string; count: number; totalAmount: number }

export type CustomerPurchaseInfo = {
  date: string
  quantity: number
  product: string
  price: number
  imgSrc: string
}
